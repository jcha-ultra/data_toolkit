import pandas as pd

# Convert a column level to a row index.
pd.DataFrame.stack

# Replace values in a column based on a specific word.
pd.Series.str.replace(r'\b' + search_term + r'\b', replace_term)
